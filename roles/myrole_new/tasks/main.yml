---
- name: Name
  shell: uname -a
  register: uname
  ignore_errors: yes
- debug: msg={{uname.stdout}}

- set_fact:
    var1: "{{uname.stdout}}"
- template:
    src: "myrolenew.j2"
    dest: "/home/cloud_user/RohithKarraInterview/roles/myrole_new/files/my-template.txt"
